/*
 * These functions are functions that'll be used throughout our program.
 * We decided to have all the functions we might use more than once in the
 * same file to make our code more readable.
 */

void initSemaphore(int, int);
void initMailbox(key_t cle);
